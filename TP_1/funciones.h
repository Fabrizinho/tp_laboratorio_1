#ifndef FUNCIONES_H_INCLUDED
#define FUNCIONES_H_INCLUDED
/** \brief Se realiza la operacion de sumar dos numeros.
 *
 * \param Se toma el primer numero.
 * \param Se toma el segundo numero.
 * \return Se muestra el resultado de la operacion.
 *
 */

float suma (float a ,float b );


/** \brief Se realiza la operacion de restar dos numeros.
 *
 * \param Se toma el primer numero.
 * \param Se toma el segundo numero.
 * \return Se muestra el resultado de la operacion.
 *
 */
float resta (float a ,float b );


/** \brief Se realiza la operacion de multiplicar dos numeros.
 *
 * \param Se toma el primer numero.
 * \param Se toma el segundo numero.
 * \return Se muestra el resultado de la operacion.
 *
 */
float multiplicar (float a ,float b );


/** \brief Se realiza la operacion de dividir dos numeros.
 *
 * \param Se toma el primer numero.
 * \param Se toma el segundo numero.
 * \return Se muestra el resultado de la operacion.
 *
 */
float dividir (float a ,float b );


/** \brief Se realiza la operacion del factorial en un numero.
 *
 * \param Se toma el numero.
 * \return Se muestra el resultado de la operacion.
 *
 */
int factorial(int n);
#endif // FUNCIONES_H_INCLUDED
